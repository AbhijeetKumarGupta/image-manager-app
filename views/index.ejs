<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <style>
      img {
        width: 100%;
      }
      button {
        width: 100%;
      }
    </style>
    <title>Home</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-6 m-auto">
          <h1 class="text-center display-4 my-4">Upload Image</h1>
          <form action="/upload" method="post" enctype="multipart/form-data">
            <label for="password">Password:</label>
            <input
              type="password"
              name="password"
              id="password"
              class="mx-5"
              style="border: 1px solid Black; border-radius: 5px; width: 52%"
            />
            <br/>
            <label for="file" class="custom-file-label">Uploader:</label>
            <input
              type="file"
              name="file"
              id="file"
              class="custom-file-input mx-5"
              style="border: 1px solid Black; border-radius: 5px"
            />
            <input
              type="submit"
              value="Submit"
              class="btn btn-primary btn-block"
            />
          </form>
          <hr />
          <% if(files){%> <% files.forEach(function (file, index){ %> <%= file.filename
          %>
          <div class="card mb-3">
            <img src="image/<%= file.filename %>" alt="" srcset="" class="mb-3"/>
            <form action="/files/<%= file._id%>" method="post" enctype="multipart/form-data">
              <label for="password" id="password_label">Password:</label>
              <input
                type="password"
                name="password"
                id="password"
                style="border: 1px solid Black; border-radius: 5px; width: 100%;"
              />
              <button type="submit" class="btn btn-danger btn-block mt-4">Delete</button>
            </form>
          </div>
          <% }) %> <% }else{ %>
          <p>No Files</p>
          <% } %>
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
